$(document).ready(function () {
    $('.summernote').summernote({
        height: 300, // inaltimea colaboratorului
        placeholder: 'Continut produs…',
        tabsize: 2,
        minHeight: 200,
        focus: true,
    });
});